FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache curl

COPY package*.json ./
RUN npm ci

COPY tsconfig.json ./
COPY src/ src/

EXPOSE 3000 3001

ENV NODE_ENV=development

CMD ["npm", "run", "dev"]
